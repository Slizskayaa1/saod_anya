#include <iostream>
#include "utils.h"

using namespace std;

int main(){
    // int x, y;
	// cout << "x = ? "; cin >> x; 
	// cout << "y = ? "; cin >> y; 
    // cout << x << " + " << y << " = " << x + y << endl;

    int x = 3, y = 5;
    cout << x + y << endl;
    cout << Plus(x, y);

    return 0;
}

